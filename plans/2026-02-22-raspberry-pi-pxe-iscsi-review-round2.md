# Plan: Raspberry Pi PXE + iSCSI 記事レビュー修正（第 2 ラウンド）

## Context

記事 `articles/raspberry-pi-pxe-iscsi-boot.md` の第 2 ラウンドレビューで 8 つの修正点が指摘された。前回ラウンドでシリアル番号・MAC アドレス・Pi の IP アドレスはダミー化済み。今回は NAS IP のダミー化、セクション削除・追加、説明の補強を行う。

## 修正一覧

### 1. ネットワークブートの利点追加（L31 付近）

USB SSD ブートと比較して、各 Pi に接続する USB ケーブルと外付け SSD が不要になる利点を追加。

```markdown
- **周辺機器・ケーブルの削減**: USB SSD ブートと比べて、各 Pi に USB ケーブルや外付け SSD を接続する必要がない。物理的な接続点が減ることで、ケーブル抜けや接触不良による障害リスクも下がる
```

### 2. ダミー値の但し書き追加（ハードウェア表の直後）

IP アドレス・MAC アドレス・シリアル番号がダミーであることを明示する `:::message` ブロックをハードウェア表の直後（L66 付近）に追加。

```markdown
:::message
**本記事の IP アドレス・MAC アドレス・シリアル番号について**
本記事に記載している IP アドレス、MAC アドレス、シリアル番号はすべてダミー値です。実際の環境に合わせて読み替えてください。
:::
```

### 3. NAS IP アドレスのダミー化（記事全体）

`192.168.0.71` → `192.168.0.100` に一括置換（13 箇所）。
ルーター `192.168.0.1` はそのまま維持。

### 4. Docker vs NAS ネイティブの説明補強（L120 付近）

Docker Compose セクション冒頭を拡充し、NAS のネイティブ Docker 機能で dnsmasq/NFS を動かしていること、iSCSI は NAS ネイティブ機能であることを明確化。

**現状**: `dnsmasq（Proxy DHCP + TFTP）と NFS サーバーを Docker Compose で管理しています。`

**修正後**:
```
UGreen NAS にはネイティブの Docker 機能が搭載されているため、dnsmasq（Proxy DHCP + TFTP）と NFS サーバーを Docker Compose で管理しています。一方、iSCSI Target は NAS のネイティブ機能として動作しており、Docker とは独立しています。
```

### 5. /boot/firmware を NFS でマウントする理由の補強（L162-165）

現在の説明が抽象的で理由が伝わりにくい。`apt upgrade` でカーネルが更新された際の具体的なフローを追記。

**修正後**:
```markdown
:::message
**NFS サーバーの役割**
root ファイルシステムは iSCSI 経由ですが、`/boot/firmware` は NFS でマウントしています。

Raspberry Pi OS で `apt upgrade` によりカーネルがアップデートされると、新しいカーネルイメージ（`kernel8.img`）や initramfs は `/boot/firmware` に書き込まれます。この `/boot/firmware` を NFS 経由で TFTP ディレクトリにマウントしておくことで、Pi 上でカーネルが更新されると TFTP サーバー上のブートファイルも自動的に更新されます。手動で TFTP ディレクトリにファイルをコピーする必要がなくなり、次回起動時から更新後のカーネルで起動します。
:::
```

### 6. DRM VC4 V3D ドライバの説明追加（L385-387）

config.txt 内のコメントを「任意」と明示し、コードブロック直後に説明文を追加。

**config.txt 内コメント修正**:
```ini
# DRM VC4 V3D ドライバ（任意: Raspberry Pi OS のデフォルト設定。
# ヘッドレス運用で GUI を使わない場合は省略可）
dtoverlay=vc4-kms-v3d
max_framebuffers=2
```

**コードブロック直後に追加**:
```
`dtoverlay=vc4-kms-v3d` と `max_framebuffers=2` は Raspberry Pi OS のデフォルト config.txt に含まれる GPU ドライバ設定です。デスクトップ環境やディスプレイ出力を利用する場合に必要ですが、ヘッドレス（CLI のみ）で運用する場合は省略しても動作します。本記事ではデフォルト設定を維持していますが、不要であれば削除して構いません。
```

### 7. 「よくある問題と対処法」セクション削除（L472-521）

- `## よくある問題と対処法` とその全サブセクション（4 つ）を削除
- 親見出し `# 動作確認とトラブルシューティング` → `# 動作確認` にリネーム
- `## 起動手順` と `## 動作確認コマンド` は維持

### 8. 「運用してみて」セクション追加（`# まとめ` の直前）

NAS・スイッチングハブが SPOF になるデメリットと、SD カードから解放された利点の両面を記述。

```markdown
# 運用してみて

PXE + iSCSI 構成に移行してから数ヶ月が経過した時点での所感をまとめます。

## 良かった点

- **SD カードの心配から解放された**: 停電やカーネルパニック後に「SD カードが壊れていないか」と確認する作業がなくなった。精神的な負担が大きく減った
- **集中管理の恩恵**: OS のアップデートやバックアップを NAS 側で一元管理できるため、3 台個別に SD カードを抜き差しする必要がない
- **パフォーマンスの向上**: iSCSI + SSD の組み合わせにより、SD カード時代と比べて体感でも I/O が高速になった

## 注意すべき点

一方で、この構成には **NAS とスイッチングハブが単一障害点（SPOF）になる** という明確なデメリットがあります。

- **NAS が停止した場合**: iSCSI Target と TFTP/NFS がすべて NAS 上で動作しているため、NAS がダウンすると全 Pi が起動不能になる。稼働中の Pi も root ファイルシステムにアクセスできなくなり、事実上停止する
- **スイッチングハブが故障した場合**: ネットワーク経由でのブートと root ファイルシステムアクセスの両方が断たれるため、同様に全 Pi が停止する

SD カードでは各 Pi が独立して動作できていたことを考えると、可用性の観点ではトレードオフになっています。自宅用途では NAS やスイッチングハブの故障頻度は低いため許容していますが、この SPOF を認識した上で運用することが重要です。
```

## 対象ファイル

- `articles/raspberry-pi-pxe-iscsi-boot.md`

## 実行順序

1. NAS IP 一括置換（#3）
2. トラブルシューティング削除 + 見出しリネーム（#7）
3. ネットワークブート利点追加（#1）
4. ダミー値但し書き追加（#2）
5. Docker/NAS ネイティブ説明補強（#4）
6. NFS 説明補強（#5）
7. DRM VC4 V3D 説明追加（#6）
8. 「運用してみて」セクション追加（#8）

## 検証

1. `pnpm exec textlint ./articles/raspberry-pi-pxe-iscsi-boot.md` で校正チェック
2. `grep -n '192.168.0.71' articles/raspberry-pi-pxe-iscsi-boot.md` で NAS IP 残存確認（ゼロであること）
3. 記事構成が正しいことの目視確認（トラブルシューティング削除、新セクション追加）
